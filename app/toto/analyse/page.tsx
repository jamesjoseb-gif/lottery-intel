import type { Metadata } from "next";
import Link from "next/link";
import { analyseTotoNumbers, type TotoResearchMode } from "@/lib/toto-analysis";
export const metadata:Metadata={title:"Analyse My TOTO Bet | Lottery Intel",description:"Enter intended TOTO numbers, choose Match 2/3/4 or System research, and get a transparent historical research assessment."};
const modes=[{value:"match2",label:"Match 2",note:"Pair relationship research"},{value:"match3",label:"Match 3",note:"Triple relationship consensus"},{value:"match4",label:"Match 4",note:"High-variance quadruple research"},{value:"standard",label:"Standard TOTO",note:"Six-number research"},{value:"system",label:"System",note:"Pool and cost comparison"}]as const;type Props={searchParams:Promise<{mode?:string;numbers?:string;budget?:string}>};
export default async function TotoAnalysePage({searchParams}:Props){const p=await searchParams,selected=(modes.some(m=>m.value===p.mode)?p.mode:"match3")as TotoResearchMode,budget=Math.max(1,Math.min(10000,Number(p.budget??50)||50)),raw=(p.numbers??"").split(/[\s,]+/).filter(Boolean),numbers=[...new Set(raw.map(Number).filter(n=>Number.isInteger(n)&&n>=1&&n<=49))].slice(0,12),required=selected==="match2"?2:selected==="match3"?3:selected==="match4"?4:6,enough=numbers.length>=required,analysis=enough?await analyseTotoNumbers(numbers,selected,budget):null;return <main className="container page-shell"><nav className="number-nav"><Link href="/">Home</Link><span>/</span><Link href="/toto">TOTO</Link><span>/ Analyse My Bet</span></nav><span className="eyebrow">TOTO Intelligence</span><h1>Analyse my TOTO bet</h1><p className="section-copy">Enter the numbers you are thinking of using. Lottery Intel compares their historical number and relationship behaviour, explains the score and helps you decide how much of your maximum budget to deploy. The Research Score is not a probability of winning.</p><form className="lucky-form" method="get"><label><span>Your intended / favourite numbers</span><input name="numbers" type="text" placeholder="e.g. 3, 10, 18, 27, 32, 41" defaultValue={numbers.join(", ")}/></label><label><span>What are you targeting?</span><select name="mode" defaultValue={selected}>{modes.map(m=><option key={m.value} value={m.value}>{m.label} — {m.note}</option>)}</select></label><label><span>Maximum budget (S$)</span><input name="budget" type="number" min="1" max="10000" step="1" defaultValue={budget}/></label><button type="submit">Research my bet</button></form>{!numbers.length?<section className="ranking-method"><h2>Already have numbers in mind?</h2><p>Enter at least {required} unique numbers. The analyser will score the relationships inside your set, identify stronger and weaker combinations and explain the historical evidence behind them.</p></section>:!enough?<p className="state state-error">Add at least {required} unique numbers for {modes.find(m=>m.value===selected)?.label} research.</p>:analysis?.error||!analysis?.data?<p className="state state-error">{analysis?.error??"Analysis is temporarily unavailable."}</p>:<ResultCard data={analysis.data} numbers={numbers} budget={budget}/>}<aside className="rankings-warning"><strong>Research score ≠ chance of winning.</strong> TOTO draws remain random. Historical relationships can help compare and structure intended selections, but they do not guarantee future results. Lottery Intel treats your budget as a maximum and may recommend leaving part of it unused.</aside></main>}
function ResultCard({data,numbers,budget}:{data:NonNullable<Awaited<ReturnType<typeof analyseTotoNumbers>>["data"]>;numbers:number[];budget:number}){const label=modes.find(m=>m.value===data.mode)?.label,fmt=(ns:number[])=>ns.map(n=>String(n).padStart(2,"0")).join(" · ");return <><section className="ranking-method"><span className="eyebrow">Your research result · data through {data.asOf}</span><h2>Research Score: {data.researchScore}/100</h2><p><strong>{data.evidence}</strong> · {label} · Maximum budget S${budget}</p><p>{data.explanation}</p><p><strong>AI budget view:</strong> deploy up to S${data.suggestedDeployment}; keep S${Math.max(0,budget-data.suggestedDeployment)} uncommitted unless you deliberately choose more exposure.</p></section><section className="ranking-method"><h2>Your numbers</h2><p>{fmt(numbers)}</p><div className="stats-preview">{data.numberScores.map(n=><div key={n.number}><strong>{String(n.number).padStart(2,"0")}</strong><span>Number score {n.score}/100</span><span>{n.appearances90} appearances / 90d</span><small>{n.appearances365} appearances / 365d</small></div>)}</div></section>{!!data.strongest.length&&<section className="ranking-method"><h2>Strongest relationships in your bet</h2>{data.strongest.map((r,i)=><p key={r.numbers.join("-")}><strong>#{i+1} {fmt(r.numbers)} — {r.score}/100</strong><br/>Historical relationship rank #{r.rankPrimary}{r.rankSecondary?` / consensus rank #${r.rankSecondary}`:""}; observed {r.countPrimary}{r.countSecondary!==undefined?` / ${r.countSecondary}`:""} times in the model windows.</p>)}</section>}{!!data.weakest.length&&<section className="ranking-method"><h2>Relationships to review</h2>{data.weakest.map(r=><p key={r.numbers.join("-")}><strong>{fmt(r.numbers)} — {r.score}/100</strong><br/>This relationship is weaker relative to the current historical comparison set.</p>)}</section>}{!!data.alternatives.length&&<section className="ranking-method"><span className="eyebrow">Favourite + AI</span><h2>Can one change improve your research score?</h2><p>These alternatives keep all but one of your chosen numbers and test every unused TOTO number as a replacement. They are historical comparisons, not predictions.</p>{data.alternatives.slice(0,3).map((a,i)=><p key={`${a.replace}-${a.with}-${i}`}><strong>Option {i+1}: replace {String(a.replace).padStart(2,"0")} with {String(a.with).padStart(2,"0")}</strong><br/>{fmt(a.resultingNumbers)} · score {a.scoreBefore} → {a.scoreAfter} ({a.improvement>0?"+":""}{a.improvement})</p>)}</section>}</>}
