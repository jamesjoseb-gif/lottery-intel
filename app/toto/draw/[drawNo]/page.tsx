import type { Metadata } from "next"; import Link from "next/link"; import { notFound } from "next/navigation"; import { TotoResults } from "@/components/Archive"; import { formatDrawDate, getDraw, type TotoRow } from "@/lib/results";
type Props = { params: Promise<{ drawNo: string }> };
export const dynamic = "force-dynamic";
export async function generateMetadata({ params }: Props): Promise<Metadata> { const { drawNo } = await params; return { title: `TOTO Draw ${decodeURIComponent(drawNo)} Results`, description: `Published Singapore TOTO winning numbers for draw ${decodeURIComponent(drawNo)}.` }; }
export default async function Page({ params }: Props) { const { drawNo } = await params; const result = await getDraw<TotoRow>("toto", decodeURIComponent(drawNo)); if (!result.data) notFound(); const { draw, rows } = result.data; return <div className="container page-shell draw-page"><Link className="back-link" href="/toto">← TOTO archive</Link><span className="eyebrow">Published TOTO draw</span><h1>Draw {draw.draw_no}</h1><p className="draw-meta">{formatDrawDate(draw.draw_date)}</p><section className="data-panel"><h2>Full results</h2><TotoResults rows={rows} /></section></div>; }
